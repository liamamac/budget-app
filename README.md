To run project:

1. Install backend dependencies:
npm install
2. Install frontend dependencies:
cd client
npm install
3. Start backend (from project root):
node server.js
4. Start frontend (from client folder):
cd client
npm start